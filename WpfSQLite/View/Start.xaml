<Page x:Class="WpfSQLite.View.Start"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      Style="{StaticResource GenericPageStyle}"
      d:DesignWidth="{Binding Source={x:Static SystemParameters.FullPrimaryScreenWidth}}"
      d:DesignHeight="{Binding Source={x:Static SystemParameters.FullPrimaryScreenHeight}}"
      Title="Start">

   <Grid>
      <Grid.RowDefinitions>
         <RowDefinition Height="100*"/>
         <RowDefinition Height="10*"/>
      </Grid.RowDefinitions>
      <Grid Row="1">
         <Grid.RowDefinitions>
            <RowDefinition Height="49*"/>
            <RowDefinition Height="5*"/>
         </Grid.RowDefinitions>
         <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="19*"/>
            <ColumnDefinition Width="140*"/>
         </Grid.ColumnDefinitions>
         <Viewbox Grid.Row="0" Grid.Column="1" >
            <Button Name="back" Content="Back" Style="{StaticResource MetroButton}"  MaxHeight="20" MinWidth="50" Click="Back_OnClick"></Button>
         </Viewbox>
      </Grid>
   </Grid>
</Page>
